class Newindex < Dynasnip
  def get(*args)
    list = app.soup.instance_eval { @backend }.send(:all_snips).sort_by { |a| a.updated_at || Time.at(0) }.reverse.map { |snip|
      "#{link_to snip.name}<br />"
    }
    "#{list}"
  end
end

Newindex

:name: newindex
:render_as: Ruby